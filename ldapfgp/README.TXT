This plugin places the code from:

  http://vanalboom.org/node/15
  
into a Trac Component, which  patches the existent functionality of the
original ldapplugin (http://trac-hacks.org/wiki/LdapPlugin).

If the component is enabled, the fine grained permissions are retrieved via
LDAP.

